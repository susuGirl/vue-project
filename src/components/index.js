// 将需要项目开启时，就全局注册的组件引入到这个index.js文件中，后续在其他地方直接引用即可，无需再import引入
// 如果不需要全局注册，封装完的组件，请不要加载到这个index.js文件中，只需在需要用到该组件的地方import引入即可

// import helloWorld from 'components/HelloWorld'

export default {
  // helloWorld
}
